# Python-pySerial-arduino-Morsecode

A simple script that converts a word into mosecode and send to arduino by serial port

Maincode.py (Change vars "port" and "speed" as needed by your arduino project)
